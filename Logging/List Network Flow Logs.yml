info:
  name: List Network Flow Logs
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/tailnet/{{tailnet}}/logging/network"
  auth:
    type: bearer
    token: "{{access_token}}"
  params:
    - name: start
      value: ""
      enabled: false
    - name: end
      value: ""
      enabled: false

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
