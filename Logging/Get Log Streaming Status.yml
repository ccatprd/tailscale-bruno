info:
  name: Get Log Streaming Status
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/tailnet/{{tailnet}}/logging/{{log_type}}/stream/status"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
