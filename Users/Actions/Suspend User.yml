info:
  name: Suspend User
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/users/{{user_id}}/suspend"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
