info:
  name: Restore User
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/users/{{user_id}}/restore"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
