info:
  name: Approve User
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/users/{{user_id}}/approve"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
