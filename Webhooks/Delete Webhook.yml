info:
  name: Delete Webhook
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/webhooks/{{endpoint_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
