info:
  name: Delete User Invite
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/user-invites/{{user_invite_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
