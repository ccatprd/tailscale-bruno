info:
  name: Delete Device Invite
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/device-invites/{{device_invite_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
