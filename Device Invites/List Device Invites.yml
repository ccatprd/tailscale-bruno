info:
  name: List Device Invites
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/device/{{device_id}}/device-invites"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
