info:
  name: List DNS Search Paths
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/tailnet/{{tailnet}}/dns/searchpaths"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
