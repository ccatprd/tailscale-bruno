info:
  name: Get DNS Preferences
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/tailnet/{{tailnet}}/dns/preferences"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
