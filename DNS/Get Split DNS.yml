info:
  name: Get Split DNS
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/tailnet/{{tailnet}}/dns/split-dns"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
