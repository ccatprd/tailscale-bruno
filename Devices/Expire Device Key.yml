info:
  name: Expire Device Key
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/device/{{device_id}}/expire"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
