info:
  name: Get Device
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/device/{{device_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
