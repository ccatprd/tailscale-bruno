info:
  name: Delete Device Posture Attribute
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/device/{{device_id}}/attributes/{{attribute_key}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
