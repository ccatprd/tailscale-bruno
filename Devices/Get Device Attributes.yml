info:
  name: Get Device Attributes
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/device/{{device_id}}/attributes"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
