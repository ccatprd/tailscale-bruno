info:
  name: Delete Service
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/tailnet/{{tailnet}}/services/{{service_name}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
