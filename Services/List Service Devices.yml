info:
  name: List Service Devices
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/tailnet/{{tailnet}}/services/{{service_name}}/devices"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
