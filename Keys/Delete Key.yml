info:
  name: Delete Key
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/tailnet/{{tailnet}}/keys/{{key_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
